apiVersion: v1
kind: Service
metadata:
  name: toastify0
  labels:
    app.kubernetes.io/name: toastify0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: toastify0
  ports:
    - name: http
      port: 80
      targetPort: http
